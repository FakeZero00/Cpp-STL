=====================================
저장시간 - 2026-05-20 17:01:10.9539020
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	char c{};
	cout << "찾을 문자를 입력하시오: ";
	cin >> c;

	XString::iterator p = find(xs.begin(), xs.end(), c);
	if (p != xs.end()) {
		cout << c << "는 " << p - xs.begin() << "번째 문자입니다." << endl;
	}
	else {
		cout << c << "는 없는 문자입니다." << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-20 17:03:25.4412329
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	char c{};
	cout << "찾을 문자를 입력하시오: ";
	cin >> c;

	XString::iterator p = find(xs.begin(), xs.end(), c);
	if (p != xs.end()) {
		cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
	}
	else {
		cout << c << "는 없는 문자입니다." << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:25:10.3862042
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 짝수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "4165416541634864153416584168468741231653168797498641536851879" };

	find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		if (0 == i % 2)
			return true;
		});

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:27:37.1216030
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 짝수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "4165416541634864153416584168468741231653168797498641536851879" };

	find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		if (0 == i % 2)
			return true;
		});

	cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:32:40.9644023
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 짝수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "4165416541634864153416584168468741231653168797498641536851879" };

	auto p = find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		if (0 == i % 2)
			return true;
		});

	cout << *p << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:33:06.1414003
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 짝수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "1165416541634864153416584168468741231653168797498641536851879" };

	auto p = find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		if (0 == i % 2)
			return true;
		});

	cout << *p << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:33:51.9193702
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "20260526" };

	auto p = find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		if (0 != i % 2)
			return true;
		});

	cout << *p << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:36:32.4219264
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "20260526" };

	auto p = find_if(xs.begin(), xs.end(), [](char c) {
		int i = c - '0';
		//if (0 != i % 2)
		if (i & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다.
			return true;
		});

	cout << *p << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:40:29.2879058
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "20260526" };

	XString::iterator p = find_if(xs.begin(), xs.end(), [](char c) {
		//int i = c - '0';
		//if (0 != i % 2)
		if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
			return true;
		return false;
		});

	if ( p != xs.end() )
		cout << distance(xs.begin(), p) + 1<< "에서 홀 수 발견: " << * p << endl;
	else 
		cout << "XS는 홀수가 없습니다." << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:43:20.3358019
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return first;
}

template <class IT, class VAL>
IT my_find_if(IT first, IT last, VAL val)
{
	while (first != last) {
		if (val(*first))
			return first;
		++first;
	}
	return first;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "20260526" };

	XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
		//int i = c - '0';
		//if (0 != i % 2)
		if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
			return true;
		return false;
		});

	if ( p != xs.end() )
		cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	else 
		cout << "XS는 홀수가 없습니다." << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:48:43.6576853
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	XString xs{ "20260526" };

	XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
		//int i = c - '0';
		//if (0 != i % 2)
		if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
			return true;
		return false;
		});

	if ( p != xs.end() )
		cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	else 
		cout << "XS는 홀수가 없습니다." << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:53:08.8989367
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:57:58.5083226
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout, " **** " });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 10:58:08.2536620
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout, " - " });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:01:05.7701853
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first;
		++first;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:01:18.5833901
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first;
		++first;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout, "-" });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:06:59.6505876
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };

	my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:08:53.2625340
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	for (char c : v)
		cout << c;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:09:14.3389576
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.resize(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	for (char c : v)
		cout << c;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:10:07.7347696
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	for (auto p = v.begin(); p != v.end(); ++p)
		cout << *p;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:13:28.5267154
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	for (auto p = v.begin(); p != v.end(); ++p)
		cout << *p;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:14:29.4125458
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	for (int i = 0; i < v.size(); ++i)
		cout << v[i];
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:16:31.4687781
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	my_copy(xs.begin(), xs.end(), v.begin());

	cout << *v.begin() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:19:10.1979876
=====================================

//Algorithm
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	my_copy(xs.begin(), xs.end(), back_inserter(v));

	for(char c: v)
		cout << c;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:46:15.0670994
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 11:59:35.0592163
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
template <>
struct less<XString> {
	bool operator() (const XString& a, const XString b) const {
		return a.size() < b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 12:03:07.9974650
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString b) const {
//		return a.size() < b.size();
//	}
//};



extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 12:05:43.8841393
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
template <>
struct less<XString> {
	bool operator() (const XString& a, const XString b) const {
		return a.size() < b.size();
	}
};



extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 12:09:19.6500807
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
template <>
struct less<XString> {
	bool operator() (const XString& a, const XString b) const {
		//사전식 비교
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
	}
};



extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 12:10:22.5250110
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
template <>
struct less<XString> {
	bool operator() (const XString& a, const XString& b) const {
		//사전식 비교
		return lexicographical_compare(a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
	}
};



extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-26 12:11:23.9992001
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
template <>
struct less<XString> {
	bool operator() (const XString& a, const XString& b) const {
		//사전식 비교
		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
	}
};



extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString, less<XString>> s{ istream_iterator<XString>{in}, {} };

	for (const XString& xs : s)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 15:45:49.6704178
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	ifstream in{ "main.cpp" };
	if (not in)
		return 20260526;
	set<XString> s{ istream_iterator<XString>{in}, {} };
	vector<XString> v{ s.begin(), s.end() };

	for(const XString& xs : v)
		cout << xs << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:15:54.6850184
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	//a -
	//b -

	map<char, int> m;
	char c;
	while (in.get(c)) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:17:35.3936061
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	//a -
	//b -

	array<int, 26> alphaNum;

	char c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in.get(c)) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:17:50.5785276
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	//a -
	//b -

	array<int, 26> alphaNum;

	char c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in.get(c)) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:21:38.3333119
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	//a -
	//b -

	array<int, 26> alphaNum;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:21:49.7642980
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:23:04.4115909
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:25:28.2652589
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:30:09.2085896
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	sort(alphaNum.begin(), alphaNum.end(), greater<int>());	//내림차순 정렬
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:30:50.9816411
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	c = 'a';
	//sort(alphaNum.begin(), alphaNum.end(), greater<int>());	//내림차순 정렬
	for (int num : alphaNum) {
		cout << c++ << " - " << num << endl;
	}

	/*map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}*/

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:32:29.3458092
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.
	//c = 'a';
	//for (int num : alphaNum) {
	//	cout << c++ << " - " << num << endl;
	//}

	map<char, int> m;
	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			++m[c];
		}
	}

	for (const auto& [c, n] : m) {
		cout << c << " - " << n << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:37:17.5833962
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	//c = 'a';
	//for (int num : alphaNum) {
	//	cout << c++ << " - " << num << endl;
	//}
	//이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	map<int, char, less<int>> icmap;

	for (int i = 0; i < alphaNum.size(); ++i) {
		icmap.insert(make_pair(alphaNum[i], 'a' + i));
	}

	for (const auto& [count, c] : icmap) {
		cout << c << '-' << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:37:55.7528378
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	//c = 'a';
	//for (int num : alphaNum) {
	//	cout << c++ << " - " << num << endl;
	//}
	//이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	map<int, char, less<int>> icmap;

	for (int i = 0; i < alphaNum.size(); ++i) {
		icmap.insert(make_pair(alphaNum[i], 'a' + i));
	}

	for (const auto& [count, c] : icmap) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:38:20.7749691
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	//c = 'a';
	//for (int num : alphaNum) {
	//	cout << c++ << " - " << num << endl;
	//}
	//이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	map<int, char, greater<int>> icmap;

	for (int i = 0; i < alphaNum.size(); ++i) {
		icmap.insert(make_pair(alphaNum[i], 'a' + i));
	}

	for (const auto& [count, c] : icmap) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:38:51.6023498
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	//알파벳의 개수만 센다. 대문자는 소문자로 변환
	// 많이 출현한 순서대로 화면에 출력하라
	//a -
	//b -

	array<int, 26> alphaNum{};

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			alphaNum[c - 'a']++;
		}
	}

	//c = 'a';
	//for (int num : alphaNum) {
	//	cout << c++ << " - " << num << endl;
	//}
	//이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	map<int, char, greater<int>> icmap;

	for (int i = 0; i < alphaNum.size(); ++i) {
		icmap.insert(make_pair(alphaNum[i], 'a' + i));
	}

	for (const auto& [count, c] : icmap) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:55:28.9342877
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n)
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	map<int, char, greater<int>> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 16:58:30.7778750
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	map<int, char, greater<int>> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap.insert({ count, c });
	}

	for (const auto& [count, c] : icmap) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:00:39.3676973
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:03:39.1551597
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제] 소설에 사용된 단어와 개수를 출력하라.
	map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:03:57.0141113
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제] 소설에 사용된 단어와 개수를 출력하라.
	map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:04:59.3312779
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제] 소설에 사용된 단어와 개수를 출력하라.
	map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:06:58.3793505
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	set<XString> s{ istream_iterator<XString>{in}, {} };
	cout << "단어의 개수: " << s.size() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:07:06.4017444
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	set<XString> s{ istream_iterator<XString>{in}, {} };
	cout << "단어의 개수: " << s.size() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:07:49.6767290
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	set<XString> s{ istream_iterator<XString>{in}, {} };
	cout << "단어의 개수: " << s.size() << endl;

	for(const XString& xs : s)
		cout << xs << endl;


	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:08:22.4366039
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	multiset<XString> s{ istream_iterator<XString>{in}, {} };
	//cout << "단어의 개수: " << s.size() << endl;

	for(const XString& xs : s)
		cout << xs << endl;


	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:08:49.9451099
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	multiset<XString> s{ istream_iterator<XString>{in}, {} };

	for(const XString& xs : s)
		cout << xs << endl;
	cout << "단어의 개수: " << s.size() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-27 17:09:17.7381289
=====================================

//Iterator2 + Container3
#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <map>
#include <array>
#include <fstream>
#include <ranges>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
// C++ STL을 떠받치는 3가지 요소: Container - Iterator - Algorithm
// Algorithm은 반복자로 코딩되어 있고 크게 3가지로 분류할 수 있다.
// 1. Non-modifying 알고리즘 : 원소 수정 X
// 2. Modifying 알고리즘 : 원소 수정 O
// 3. Sort and Related 알고리즘 : 정렬과 정렬 관련 알고리즘
// 
// STL Associative(연관) Container - 해싱을 위한 컨테이너, key - value pair, O(log n), index로 access하지 않는 컨테이너
// set, multiset	- key == value
// map, multimap	- key를 정렬 기준으로 value를 검색
//================================================================

template <class IT, class VAL>
IT my_find(IT first, IT last, VAL val)
{
	while(first != last) {
		if (*first == val)
			return first;
		++first;
	}
	return last;
}

template <class IT, class PRED>
IT my_find_if(IT first, IT last, PRED pred)
{
	while (first != last) {
		if (pred(*first))
			return first;
		++first;
	}
	return last;
}

template <class IT, class OUT>
OUT my_copy(IT first, IT last, OUT out)
{
	while (first != last) {
		*out++ = *first++;
	}
	return out;
}

//std::less를 XString에 대해서 Specialization
//template <>
//struct less<XString> {
//	bool operator() (const XString& a, const XString& b) const {
//		//사전식 비교
//		return lexicographical_compare(a.begin(), a.end(), b.begin(), b.end());
//	}
//};

class Dog {
public:
	bool operator() (const XString& a, const XString& b) const {
		return a.size() > b.size();
	}
};

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "Iterators are a generalization of pointers" };

	//[문제] 찾는 글자가 xs에 있나 출력하라.
	//있다면 몇 번째 문자인지, 없다면 없는 문자라고 출력하라.

	/*while(true){
		char c{};
		cout << "찾을 문자를 입력하시오: ";
		cin >> c;

		XString::iterator p = my_find(xs.begin(), xs.end(), c);
		if (p != xs.end()) {
			cout << c << "는 " << distance(xs.begin(), p) + 1 << "번째 문자입니다." << endl;
		}
		else {
			cout << c << "는 없는 문자입니다." << endl;
		}
	}*/
	
	//[문제] xs의 첫번째 홀수를 찾아라(xs에는 숫자만 있다)
	//XString xs{ "20260526" };

	//XString::iterator p = my_find_if(xs.begin(), xs.end(), [](char c) {
	//	//int i = c - '0';
	//	//if (0 != i % 2)
	//	if (c & 1)	//홀수는 1로 끝나기에 1과 비트 연산을 하면 홀수인지 짝수인지 알 수 있다. 아스키 코드도 그렇게 설계되었기 때문에 가능하다.
	//		return true;
	//	return false;
	//	});

	//if ( p != xs.end() )
	//	cout << distance(xs.begin(), p) + 1<< "에서 홀수 발견: " << * p << endl;
	//else 
	//	cout << "XS는 홀수가 없습니다." << endl;

	/*XString xs{ "2026년 05워 26일 12주 2일" };
	vector<char> v;
	v.reserve(xs.size());*/

	//[문제] xs에 있는 글자들을 v에 복사하라.
	//my_copy(xs.begin(), xs.end(), ostream_iterator<XString::value_type>{ cout });
	//my_copy(xs.begin(), xs.end(), v.begin());
	//이 상태에서는 vector의 size가 0이기 떄문에 v를 그냥 출력할 수가 없다.
	//my_copy(xs.begin(), xs.end(), back_inserter(v));	//back_inserter는 vector의 뒤에 원소를 추가하는 반복자 어댑터이다. v의 size가 늘어나면서 원소가 추가된다.

	//for(char c: v)
	//	cout << c;
	//cout << endl;

	//set이 원소의 uniqueness를 판단하는 기준 - equivalence
	//equality(동등) 와 equivalence(상등성)
	//equality는 ==로 같음을 판단, equivalence는 <로 같음을 판단.
	/*set<int> s{ 2, 4, 6, 1, 3, 1, 3 };
	less<int> cmp;
	cmp(2, 4);

	for (int n : s)
		cout << n << " ";
	cout << endl;*/

	//"main.cpp"의 단어를 set<XString>에 저장하라.
	//정렬은 길이 순서로 하라

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };

	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//[문제] set에 있는지 없는지 다음과 같이 출력하라. != xs가 set에 있는지 없는지 출력하라.
	//	//있다면 - 3번째 단어입니다.
	//	//없다면 - 없는 단어입니다. set에 추가하시겠습니까?
	//	//		 - 1. 추가한다.  2. 추가하지 않는다.

	//	auto p = s.find(xs);
	//	if (p != s.end()) {
	//		cout << xs << "는 " << distance(s.begin(), p) + 1 << "번째 단어입니다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << xs << "가 s에 존재하지 않습니다. s에 추가하시겠습니까?\n1. 추가한다\n2. 추가하지 않는다" << endl;
	//		int n{};
	//		cin >> n;
	//		if (n == 1) {
	//			s.insert(xs);
	//			cout << xs << "가 s에 추가되었습니다." << endl;
	//			system("pause");
	//		}
	//		else continue;
	//	}
	//	//[문제] xs가 set에 있는지 없는지 출력하라.
	//	//if(s.contains(xs)){
	//		//cout << "있는 단어" << endl;
	//	//}
	//}

	//ifstream in{ "main.cpp" };
	//if (not in)
	//	return 20260526;
	//set<XString> s{ istream_iterator<XString>{in}, {} };
	//vector<XString> v{ s.begin(), s.end() };

	////[문제] xs가 vector에 있는지 없는지 출력하라.
	//while (true) {
	//	for (const XString& xs : s)
	//		cout << xs << endl;

	//	cout << "찾을 단어를 입력하시오: ";
	//	XString xs;
	//	cin >> xs;

	//	//모처럼 set의 정렬된 데이터를 받아왔는데 그냥 find를 쓰면 시퀀셜 서치로 찾게 되어서 비효율적이다.
	//	//데이터가 정렬되어 있다면 바이너리 서치를 하는 편이 좋다.
	//	//Vector랑 같은 구조이면서 항상 정렬된 상태를 유지하고 싶다면 flat_set이라는 컨테이너 어댑터가 있다.
	//	auto p = find(v.begin(), v.end(), xs);
	//	if (p != v.end()) {
	//		cout << "있다." << endl;
	//		system("pause");
	//	}
	//	else {
	//		cout << "없다." << endl;
	//		system("pause");
	//	}
	//}

	//ifstream in{ "이상한 나라의 앨리스.txt" };
	//if(not in)
	//	return 20260526;

	////[문제] "이상한 나라의 앨리스.txt"에 사용된 알파벳의 개수를 다음과 같이 출렧하라.
	////알파벳의 개수만 센다. 대문자는 소문자로 변환
	//// 많이 출현한 순서대로 화면에 출력하라
	////a -
	////b -

	//array<int, 26> alphaNum{};

	//char c;
	//while (in >> c) {
	//	if (isalpha(c)) {
	//		c = tolower(c);
	//		alphaNum[c - 'a']++;
	//	}
	//}

	////c = 'a';
	////for (int num : alphaNum) {
	////	cout << c++ << " - " << num << endl;
	////}
	////이 대로는 알파벳도 같이 많이 나온대로 정렬하기 힘들다. 이중배열이나 써야 할 수 있을 것이다.

	//map<int, char, greater<int>> icmap;

	//for (int i = 0; i < alphaNum.size(); ++i) {
	//	icmap.insert(make_pair(alphaNum[i], 'a' + i));
	//}

	//for (const auto& [count, c] : icmap) {
	//	cout << c << " - " << count << endl;
	//}

	/*ifstream in{ "이상한 나라의 앨리스.txt" };
	if(not in)
		return 20260526;*/

	//[문제] "이상한 나라의 앨리스.txt"에 가장 많이 사용된 문자 순으로 다음과 같이 출력하라
	//e - 3000
	//o - 2500
	//a - 2200

	/*map<int, char> icmap;
	map<char, int> cimap;

	char c;
	while (in >> c) {
		if (isalpha(c)) {
			c = tolower(c);
			cimap[c]++;
		}
	}

	for(const auto& [c, count] : cimap) {
		icmap[count] =  c;
	}

	for (const auto& [count, c] : icmap | views::reverse) {
		cout << c << " - " << count << endl;
	}*/

	ifstream in{ "이상한 나라의 앨리스.txt" };
	if (not in)
	return 20260526;

	//[문제]소설에 사용된 단어와 개수를 출력하라.
	/*map<XString, int> ximap;

	XString word;
	while (in >> word) {
		ximap[word]++;
	}

	for (const auto& [word, count] : ximap) {
		cout << word << " - " << count << endl;
	}
	cout << "단어의 개수: " << ximap.size() << endl;*/

	//[문제] set<XString>에 읽어와라.
	multiset<XString> s{ istream_iterator<XString>{in}, {} };

	for(const XString& xs : s)
		cout << xs << "\t";
	cout << endl;
	cout << "단어의 개수: " << s.size() << endl;

	save("main.cpp");
}