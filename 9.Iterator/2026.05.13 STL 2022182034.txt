=====================================
저장시간 - 2026-05-12 12:18:11.1232153
=====================================

//Iterator
#include <iostream>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================

//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.

	for (char c : xs) {
		cout << c << '-';
	}
	cout << endl;


	save("main.cpp");
}=====================================
저장시간 - 2026-05-12 12:19:05.6486411
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================

//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	sort(xs.begin(), xs.end());

	for (char c : xs)
		cout << c << '-';
	cout << endl;


	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 09:54:09.3850365
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	ostream_iterator<char> p{ cout };
	*p = 'a';
	++p;
	*p = 'B';
	++p;
	*p = '\n';

	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 09:58:11.4491598
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	ostream_iterator<char> p{ cout };
	*p = 'a';
	*p = 'B';
	*p = '\n';

	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 09:59:45.1096527
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	ostream_iterator<char> p{ cout };
	p = 'a';
	p = 'B';
	p = '\n';

	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 10:04:38.7598582
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	vector<char> v(10);
	auto p = v.begin();
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	*p++ = 'a';
	*p++ = 'B';
	*p++ = '\n';

	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 10:05:44.4399592
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	vector<char> v(10);
	auto p = v.begin();
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	*p++ = 'a';
	*p++ = 'B';
	*p++ = '\n';

	for (int i = 0; i < 10; ++i)
		cout << v[i] << endl;
	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 10:06:35.1289102
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.reserve(10);
	auto p = v.begin();
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	*p++ = 'a';
	*p++ = 'B';
	*p++ = '\n';

	for (char c : v)
		cout << c << endl;
	//2.반복자를 인자로 받는 코딩
	//3.반복자의 종류를 판단하는 방법

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 13:41:36.8376870
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	cout << typeid(iter).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법

	f(vector<int>::iterator{});
	f(list<XString>{}.begin());

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 13:43:58.1371349
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	cout << typeid(iter).name() << endl;
	//if(typeid(iter) == typeid)
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법

	f(vector<int>::iterator{});
	f(list<XString>{}.begin());

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 13:44:49.0221838
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법

	f(vector<int>::iterator{});
	f(list<XString>{}.begin());

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 13:50:02.3012302
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법

	f(vector<int>::iterator{});
	f(list<XString>{}.begin());

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 13:58:02.3670195
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 14:08:06.3487443
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	char* p;
	//f(p);
	cout << typeid(p).name() << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-14 14:12:52.1606068
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다.
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	char* p;
	//f(p);

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:33:13.3156768
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);

	ostream_iterator<char> p{ cout };

	p = 'A';
	p = 'B';
	p = 'C';

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:38:25.6636069
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);


	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.resize(10);
	auto p = v.begin();

	*p++ = 'A';
	*p++ = 'B';
	*p++ = 'C';

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:39:33.9110570
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);


	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.resize(10);
	auto p = v.begin();

	*p++ = 'A';
	*p++ = 'B';
	*p++ = 'C';

	for(const char &q : v)
		cout << q;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:39:57.9351085
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);


	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.reserve(10);
	auto p = v.begin();

	*p++ = 'A';
	*p++ = 'B';
	*p++ = 'C';

	for(const char &q : v)
		cout << q;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:40:13.9097630
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);


	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.resize(10);
	auto p = v.begin();

	*p++ = 'A';
	*p++ = 'B';
	*p++ = 'C';

	for(const char &q : v)
		cout << q;
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:45:59.4616101
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);


	//ostream_iterator<char> p{ cout };
	vector<char> v;
	v.reserve(10);
	vector<char>::iterator p{ v.begin() };

	*p++ = 'A';
	*p++ = 'B';
	*p++ = 'C';

	for(int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 10:57:39.7623320
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	f(deque<char>::reverse_iterator{});
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);

	f(ostream_iterator<int>{ cout });

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:06:54.5076690
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다.
	cout << typeid(IT::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	/*f(istream_iterator<char>{ cin });
	f(ostream_iterator<int>{ cout });
	//여기서부터는 상속관계(inheritance hierarchy)
	f(forward_list<int>::const_iterator{});
	f(list<XString>::const_reverse_iterator{});

	//여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	f(deque<char>::reverse_iterator{});
	//여기서부터는 contigous한 메모리를 사용하는
	f(vector<int>::iterator{});
	f(array<int, 0>{}.begin());*/

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//char* p;
	//f(p);

	f(ostream_iterator<int>{ cout });
	//여기서부터는 상속관계(inheritance hierarchy)
	f(istream_iterator<int>{ cin });
	f(forward_list<int>::iterator());
	f(list<char>{}.rbegin());

	//여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	f(deque<char>::reverse_iterator{});
	//여기서부터는 contigous한 메모리를 사용하는 자료형
	f(array < XString, 0>::const_reverse_iterator{});
	f(vector<XString>{}.crend());
	f(string{}.end());
	

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:23:20.5526090
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.

	XString* xp;
	f(xp);

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:30:41.8011817
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	vector<int> v;
	auto p{ v.begin() };

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.

	cout << contiguous_iterator<vector<int>::iterator> << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:33:29.2689221
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::iterator_category).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	vector<int> v;
	auto p{ v.begin() };

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.

	cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:53:00.1529320
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

//이렇게 하면 어떤 타입이든 함수에 다 들어가버린다. 이때 필요한 것이 concepts이다.
//concepts를 쓰면 함수에 필요한 자료형만 들어올 수 있도록 제한할 수 있다.
template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::value_type).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.
	/*cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;*/

	XString xs{ "Iterators are a generalization of pointers" };

	for (auto i = xs.begin(); i != xs.end(); ++i)
		cout << *i << ' ';
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:53:49.2064800
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

//이렇게 하면 어떤 타입이든 함수에 다 들어가버린다. 이때 필요한 것이 concepts이다.
//concepts를 쓰면 함수에 필요한 자료형만 들어올 수 있도록 제한할 수 있다.
template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::value_type).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.
	/*cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;*/

	XString xs{ "Iterators are a generalization of pointers" };

	for (auto i = xs.end(); i != xs.begin(); i = i - 1)
		cout << *i << ' ';
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:54:10.3196906
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

//이렇게 하면 어떤 타입이든 함수에 다 들어가버린다. 이때 필요한 것이 concepts이다.
//concepts를 쓰면 함수에 필요한 자료형만 들어올 수 있도록 제한할 수 있다.
template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::value_type).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.
	/*cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;*/

	string xs{ "Iterators are a generalization of pointers" };

	for (auto i = xs.begin(); i != xs.end(); ++i)
		cout << *i << ' ';
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 11:54:21.2610030
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

//이렇게 하면 어떤 타입이든 함수에 다 들어가버린다. 이때 필요한 것이 concepts이다.
//concepts를 쓰면 함수에 필요한 자료형만 들어올 수 있도록 제한할 수 있다.
template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::value_type).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.
	/*cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;*/

	string xs{ "Iterators are a generalization of pointers" };

	for (auto i = xs.rbegin(); i != xs.rend(); ++i)
		cout << *i << ' ';
	cout << endl;

	save("main.cpp");
}=====================================
저장시간 - 2026-05-19 12:18:04.9724277
=====================================

//Iterator
#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <forward_list>
#include <deque>
#include <array>
#include <concepts>
#include "XString.h"
#include "Save.h"
using namespace std;

//================================================================
//Iterator는 포인터를 일반화한 것으로 자료형에 구애받지 않고 똑같이 코딩할 수 있게한다(Uniform manner).
//1.반복자를 직접 사용한 코딩
//2.반복자를 인자로 받는 코딩
//3.반복자의 종류를 판단하는 방법
//반복자는 3가지의 정보를 제공한다.
//iterator_category, difference_type(반복자끼리의 연산), value_type, pointer, reference
//difference_type은 반복자끼리의 연산에 사용되는 타입으로, 예를 들어 vector<int>::iterator는 difference_type이 ptrdiff_t로 정의되어 있다.
//================================================================

extern bool lookup; //special()에서 관찰 여부를 결정하는 전역 변수

//이렇게 하면 어떤 타입이든 함수에 다 들어가버린다. 이때 필요한 것이 concepts이다.
//concepts를 쓰면 함수에 필요한 자료형만 들어올 수 있도록 제한할 수 있다.
template <class IT>
void f(IT iter)
{
	//이렇게 타입 구분하면 안된다;;
	/*cout << typeid(iter).name() << endl;
	if (typeid(iter) == typeid(std::_Vector_iterator<class std::_Vector_val<struct std::_Simple_types<int> > >))
		cout << "벡터 반복자" << endl;*/

	//반복자가 제공하는 부가 정보를 사용하여 종류를 판단한다. 다만 이렇게 판단하면 포인터는 받아올 수 없다.
	//cout << typeid(IT::iterator_category).name() << endl;
	
	//iterator_traits를 이용해서 우회로를 사용한다.
	cout << typeid(iterator_traits<IT>::value_type).name() << endl;
}

int main()
{
	//XString xs{ "the quick brown fox jumps over the lazy dog" };	//영문에서 모든 알파벳을 포함하는 문장.
	//sort(xs.begin(), xs.end());

	////range-for문을 사용하려면 반복자 인터페이스가 필요하다.
	//for (char c : xs)
	//	cout << c << '-';
	//cout << endl;

	//1.반복자를 직접 사용한 코딩 - ex)출력 반복자
	//ostream_iterator<char> p{ cout };
	/*vector<char> v;
	v.reserve(10);
	auto p = v.begin();*/
	//* 연산자나 ++연산자가 없어도 ostream_iterator는 출력 반복자이기에 출력이 가능하지만
	//만약 ostream_iterator가 아닌 Vector의 이터레이터라도 코딩이 가능하도록 하는 것이 바람직하다.
	// 반복자라면 제공해야하는 연산자로만 코딩하는 것이 좋다.
	//p.operator*(), p.operator++()
	//*p++ = 'a';
	//*p++ = 'B';
	//*p++ = '\n';

	//reserve로 용량을 확보하긴 해도 size 데이터가 0이므로 begin()과 end()가 같은 위치를 가리키게 돼서 for(char &c: v)로는 출력할 수 없다.
	/*for (int i = 0; i < v.capacity(); ++i)
		cout << v[i];
	cout << endl;*/

	//2.반복자를 인자로 받는 코딩 -> 알고리즘 함수가 하고 있다.
	//3.반복자의 종류를 판단하는 방법
	//알고리즘이 자료구조를 알 수 있다면 더 효율적으로 코딩할 수 있기 때문. distance 함수가 좋은 예
	//f(ostream_iterator<int>{ cout });
	////여기서부터는 상속관계(inheritance hierarchy)
	//f(istream_iterator<int>{ cin });
	//f(forward_list<int>::iterator());
	//f(list<char>{}.rbegin());

	////여기서부터는 []연산자를 사용할 수 있는 랜덤 반복자
	//f(deque<char>::reverse_iterator{});
	////여기서부터는 contigous한 메모리를 사용하는 자료형
	//f(array < XString, 0>::const_reverse_iterator{});
	//f(vector<XString>{}.crend());
	//f(string{}.end());
	//다만 그냥은 countigus_iterator가 나오지 않는다.

	//반복자는 포인터를 일반화 한 것이지만 그 역은 성립하지 않는다.
	//포인터는 반복자와 같이 부가정보를 가지고 있지 않다.
	/*XString* xp;
	f(xp);*/

	//p가 contigous_iterator임을 확인하는 방법
	//C++20의 concepts을 이용하면 된다.
	/*cout << "벡터 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<vector<int>::iterator> << endl;
	cout << "덱 반복자는 contiguous인가? - " << boolalpha << contiguous_iterator<deque<int>::iterator> << endl;
	cout << "덱 반복자는 bidirectional인가? - " << boolalpha << bidirectional_iterator<deque<int>::iterator> << endl;*/

	XString xs{ "Iterators are a generalization of pointers" };

	for (auto i = xs.rbegin(); i != xs.rend(); ++i)
		cout << *i << ' ';
	cout << endl;

	save("main.cpp");
}